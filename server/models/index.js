const {ProfileModel} = require('./profileModel');
const {StoriesModel} = require('./storiesModel');
const {CharactersModel} = require('./charactersModel');

module.exports = {ProfileModel, StoriesModel, CharactersModel};